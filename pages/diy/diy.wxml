<view class="container {{viewMode === 'calendar' ? 'calendar-mode' : ''}}">
  <view class="subtitle">{{currentRuleName}}</view>
  <button bindtap="toDiyPush">自定义规则提醒</button>
  <text>{{r0}}</text>
  <view class="flex-wrap">
    <button bindtap="toggleView">{{viewMode === 'calendar' ? '切回一周列表' : '切换成月日历'}}</button>
  </view>
  <block wx:if="{{viewMode === 'list'}}">
    <text>{{r1}}</text>
    <text>{{r2}}</text>
    <text>{{r3}}</text>
    <text>{{r4}}</text>
    <text>{{r5}}</text>
    <text>{{r6}}</text>
    <text>{{r7}}</text>
  </block>
  <shift-calendar wx:else cal="{{cal}}" bind:daytap="onCalendarDayTap"></shift-calendar>
  <block wx:if="{{viewMode === 'list'}}">
    <view class="flex-wrap">
      <button bindtap="backDay">前 一 天</button>
      <button bindtap="nextDay">后 一 天</button>
    </view>
    <view class="flex-wrap">
      <button class="yellow" bindtap="backWeek">上 一 周</button>
      <button class="yellow" bindtap="nextWeek">下 一 周</button>
    </view>
  </block>
  <view class="flex-wrap" wx:else>
    <button class="yellow" bindtap="backMonth">上 一 月</button>
    <button class="yellow" bindtap="nextMonth">下 一 月</button>
  </view>
  <view class="flex-wrap">
    <button bindtap="toToday" type="primary">返回今天</button>
    <button bindtap="close" type="warn">返回主页</button>
  </view>
  <view class="ad-banner">
    <ad unit-id="adunit-d5a2055026c60752"></ad>
  </view>
</view>
