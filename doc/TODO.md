# 待办

## 1. 经警队排班（上一天休三天）—— 已实现（已校准）

**这是 2.4.0 的核心目标，已完成并有测试守着。**

- [x] 班组数：**4 个班**（一班~四班）—— 已确认（2026-09-11）
- [x] 排班基准：**2026-09-10 = 一班上班** —— 已确认（2026-09-11）
- [x] 班组切换：个人页顶部按钮（一班~四班）随时切换，打开默认一班（沿用现有页面做法）
- [x] 首页单位名「热 电 厂」→「中 油 电 能」（见 `doc/changes/2026-09-11-212119-index-unit-rename.md`）
- [x] **补修：锚点日之前的日期会算错班** —— 各倒班页 `getTotalDays()` 原来用 `Math.abs()` 取天数差，
  锚点日**之前**的日期被当成正数取模，班次算反。老页面锚点在 2016 / 2017 年翻不到那么早；
  经警队锚点是 2026-09-10，用户一往前翻就错。已统一改成有符号天数差 + `shift.mod()`，
  见 `doc/changes/2026-09-11-212218-reverse-date-fix.md`

**规则**：上一天休三天 —— 4 个班循环，每天恰好 1 个班上班。
**验收**：已由 `test/shift-pages.test.js` 钉住 —— 锚点守卫、周期性不变量、
12 个与真实排班核对过的日期逐日断言、锚点前后各 200 天每天恰好 1 个班上班、
总览页与个人页一致、金标准快照。

## 2. 倒班页面「月日历」视图 —— 已完成

- [x] 法定假期数据来源：**内置 JSON**（`utils/holiday.js`），不引第三方接口
- [x] 格子内容：日期 + 该天班次文字（与列表同一套算法）+ 节假日标记；上班/休息用底色区分
- [x] 交互：页面内按钮切换；日历模式下翻页换成「上一月/下一月」；点某天回列表并从该天开始
- [x] 公共实现：`utils/calendar.js` + `components/shift-calendar/`；页面只需实现 `getDayCell()`
- [x] 接入范围：~~9 个倒班页（含 diy）~~ → **现在是 8 个**（diy 页在 2.4.0 随 DIY 链路删除）

**遗留待办**：

- [ ] **节假日数据每年更新**：国务院通知一般每年 11 月发布下一年安排，发布后补进 `utils/holiday.js` 的 `DATA`。
  目前只内置 2025、2026（都按 gov.cn 原文逐条核对过）；**2027 通知未发布，所以没有 2027 数据，
  也不要凭记忆推算**。没有数据的年份日历会提示「该年节假日数据尚未内置」
- [ ] 界面级验证待补：**格子换行与对比度已覆盖**（2026-09-21 配色改动时用 automator 逐页截过 8 页
      日历图，见 `doc/changes/2026-09-21-160500-calendar-colors.md`）；仍待真机人工确认的：
      点某天回列表的手感、新配色在真机屏幕上的实际观感

## 3. 小程序自动强制升级 —— 已完成（3 处缺陷已修）

`onUpdateReady` 注册时机、`onUpdateFailed` 兜底、入口覆盖不全，三个问题都已修，
逻辑统一收到 `app.js` 的 `initUpdateManager()`，提示改成阻塞式 `showModal`。
详见 `doc/changes/2026-09-11-212331-force-update-fix.md`。

- [x] **上传已跑通**（2026-09-14）：2.4.0 用 `cli upload` 传成**开发版本**，代码包 260.9 KB / 267178 字节。
  命令行上传的前提与两个坑（请求由 IDE 主进程发出、证书失败会伪装成权限错误）记在
  `AGENTS.md`「本机验证的边界」
- [ ] **端到端验证仍待补**：「上传 → 检测到新版本 → 弹窗 → 重启」要等这一版先成为**线上版本**、
  再上传一个更高版本才能真跑一遍。现在只验到"开发版本已生成"，`initUpdateManager()` 的三个回调
  在真机上是否按预期触发仍未证实

## 4. 发布前必须在微信后台做的事（2.5.0 已按删除后的现状更新）

代码改不动的部分都在 `doc/发布前检查单.md`：

- [ ] **request 合法域名**：已**无需配置** —— LeanCloud 统计删除后全仓库零网络请求，
      后台白名单里残留的 `api.leancloud.mp.shiftcheck.work` 可移除
- [ ] **用户隐私保护指引**：**已没有必须声明的隐私接口，也不再收集任何信息**
      （剪贴板随激活码导入页删除、邮箱手机号随推送链路删除、
      设备信息与 openid 的使用统计随 LeanCloud 集成删除、从来没有定位）
- [ ] **广告位只剩 1 个**：`adunit-499feca899514736`（banner，11 个页面在用；首页那处是注释掉的）。
      确认它属于本小程序主体且已开通流量主
- [ ] **作废两个已泄露的 ip138 token**（`12ff…8129`、`2da1…b1b72`）：代码里已删，但公开仓库的 git 历史里还在
- [x] ~~基础库版本统一~~ —— **2026-09-22 已统一到 2.25.3**：`project.config.json` 2.8.2 → 2.25.3，
      与 `project.private.config.json` 一致。作者选定保守方案（本机虽有 3.17.3，升级后表现要重验一轮，不值当）
- [ ] **GitHub Support 工单**：旧 commit 仍可按 SHA 访问。force-push 改写分支不会真正删除它们，
      要提工单（https://support.github.com/contact ，类型 Remove cached views）让 GitHub 跑 GC。
      **2026-09-22 进展**：从本地 dangling 对象扫出 **9 个文件树含完整 ip138 token 的 SHA**
      并抽验 4 个在 github.com 均仍可访问（实锤）；工单正文草稿已备好交作者，
      作者决定**发版前先提**。提完工单勾掉本条
- [x] 上传开发版本（2026-09-22，**2.5.0**，代码包 54.7 KB / 55993 字节；
      v2.5.0 tag 已移到含 #54~#62 的 master，Release 正文同步重新生成；
      2026-09-14 曾传过 2.4.0 260.9 KB，已被本次上传取代）
- [ ] 后台把 2.5.0 设为**体验版** → 按检查单第 9 节真机走一遍 → 提审 → 发布

## 5. 已删除的功能（记录备查，别以为是漏掉了）

| 删除时间 | 内容 | 原因 |
| --- | --- | --- |
| 2.4.0 | `pages/setting/authUserInfo/` | 依赖的 `scope.userInfo` 授权微信 2021 年就停止下发，且它把「我的DIY规则」「订阅上班推送」两个入口挡死 |
| 2.4.0 | `pages/setting/feedback/`、`pages/logs/` | 空占位页 / 模板默认页，都没有入口 |
| 2.4.0 | `libs/mta_analysis.js`、`libs/av-weapp-min.js` | 死文件（MTA 2.3.3 就已停用；打包版 SDK 从未被 require） |
| 2.4.0 | 推送链路 5 页：`pushHome`、`newPushMission`、`updateMail`、`updatePhone`、`accountHome` | 服务端定时任务随 `shiftcheck-server` 停用；`PushMission` 与 7 个推送字段全部只写不读；创建任务的入口自 2.2.x 起被裸 `return` 堵死 |
| 2.4.0 | DIY 链路 4 页：`diy`、`diyPush`、`importRuleByKey`、`myRuleHome` | 全仓库零处创建 `Rule`/`RuleKey`，激活码对新用户 100% 报错且无处申请；导入页还会在登录未完成时烧毁一次性激活码并提示成功 |
| 2.5.0 | LeanCloud 集成整条链路：`libs/` 两个 SDK（237.6 KB）、`app.js` 的 AV 初始化 / 登录 / `getOpenid()`、首页 `mystep2()` 使用统计上报 | 专有域名 `shiftcheck.work` 2026-08-09 到期未续费、全球解析不到（RDAP 查实），统计对所有人早已是死的；SDK 占 260.6 KB 上传包的 91%，删除后 ~25 KB。决策：不赎回域名 |

LeanCloud 后台的 `WechatUser` / `PushMission` / `Rule` / `RuleKey` / `UserRule` 表**没有删**，
数据还在。客户端自 2.5.0 起与 LeanCloud 零依赖（不读也不写），确认不需要后可自行清理。

## 6. 已知遗留（本轮明确"先记着、之后再修"，都不阻塞发布）

按性价比排序：

- [x] ~~**转发功能全死**~~ —— **2026-09-21 已修**：新增 `utils/share.js`（统一的卡片标题、
      当前页直达 path、`setup()` 带 `menus` 同时打开「转发」与「分享到朋友圈」），
      11 个页面全部接上 `onShareAppMessage` / `onShareTimeline`，`share.test.js` 钉住接线与
      标题对应。见 `doc/changes/2026-09-21-141530-share.md`。
      原条目记录的事实：此前 11 个页面都调了 `wx.showShareMenu()`，
      但 0 个页面实现 `onShareAppMessage`，按微信规则右上角根本不会出现「转发」；
      `README.md` 2.3.0 写的「给每个页面添加转发按钮」不成立
- [x] ~~**11 个页面都没有 `navigationBarTitleText`**~~ —— **2026-09-21 已补**：每个页面 json 加了自己的标题
      （倒班页「xx · 个人/总览」、设置页「设置」、默认班组页「默认班组」，首页保持「查班神器」），
      `structure.test.js` 有门禁守着新页面必须带标题。
      见 `doc/changes/2026-09-21-135549-nav-titles.md`
- [x] ~~**「个人模式默认班组」名不副实**~~ —— **2026-09-21 已修（选了「四种倒班都接上」）**：
      设置页重写成四种倒班各一行（五班三倒 5 班 / 经警队 4 班 / 四班四倒 4 班 / 三班半倒 3 班），
      各自存独立的 storage key；经警队 / 四班四倒 / 三班半倒的个人页 `onLoad`
      读自己的 key（没设置过仍是默认一班，行为不变）。五班三倒的旧逻辑未动（未设置默认三班）。
      `default-banzu.test.js` 钉住设置页与 4 个个人页的读写契约。
      见 `doc/changes/2026-09-21-153400-default-banzu.md`
- [x] ~~**四班四倒 / 三班半倒的 4 个页面没有「跳转到指定日期」**~~ —— **2026-09-21 已补**：
      4 个页面的 wxml 加 `<picker mode="date">`（位置与五班三倒页一致）、js 加逐字一致的
      `bindDateChange`；`structure.test.js` 门禁 8 个倒班页都必须有 picker，
      `shift-pages.test.js` 逐页钉住跳转行为（1 基月份转 0 基、列表从跳转日重算、
      与直接设日期同源）。见 `doc/changes/2026-09-21-143210-date-picker.md`
- [x] ~~**`pages/index/index.wxml` 用了 2 处 HTML 的 `<span>`**（单位名标签）~~ ——
      **2026-09-21 已改**：两处单位名标签统一成 `<text class="unit-label">`，
      wxss 的 `span` 选择器同步改成 `.unit-label`（字号/颜色/边距保持原值，
      行内其余表现跟随本页 text 通用规则）；`structure.test.js` 加门禁不许
      HTML 标签进 wxml。见 `doc/changes/2026-09-21-145558-index-span.md`
- [x] ~~**断网时首页会弹一次「登录没成功」**~~ —— **2026-09-14 已修**（发布前发现的最后一条阻塞项）。
      原先首页 `mystep2()` 的统计上报走"取不到 openid 就补登录、补不上就弹阻塞式 `showModal`"的路径，
      而**首页 8 个倒班入口全是纯本地计算、根本不需要登录**，用户只想查班却被拦住。
      修法比原计划更彻底：`mystep2` 改走 `app.getOpenid()` 静默路径，
      并把 `app.js` 里那对方法**整对删除**（修完零调用方，留着只是给未来一个能弹框的入口），
      `hygiene.test.js` 加门禁守着不许复活，`index-page.test.js` 加 5 条测试钉住"静默"这个性质
      （取不到 openid / undefined / 空串都不弹，取得到时照常上报，作者 openid 跳过）。
      详见 `doc/changes/2026-09-14-142621-fix-login-fail-modal.md`
- [x] ~~**⚠ LeanCloud 专有域名全球失效**~~ —— **2026-09-15 已处理**：决策**不赎回域名**，
      集成整条删除（删除清单见上面第 5 节的表，决策依据与证据链见
      `doc/changes/2026-09-15-185733-remove-leancloud.md`）。
      原条目记录的事实：`shiftcheck.work` 2026-08-09 到期未续费（RDAP 查实：
      `client transfer prohibited, pending delete, redemption period`，权威 NS 被换成
      注册商过期停放服务器），`api.leancloud.mp.shiftcheck.work` 对全球所有用户解析不到，
      统计早已是死的 —— 不是本机网络问题，也不是代理问题
- [x] ~~**月日历配色不够醒目**~~ —— **2026-09-21 已改（用户从两版截图里选定方案 B）**：
      普通日一律白底（原灰底/粉底/绿底取消），上班红字、休息绿字；
      法定节假日浅红底 + 红框 + 红日期红节日名（「哪天放假」一眼可见）；
      调休补班日白底 + 橙色「班」角标；今天绿框不变。
      备选方案 A（整格红底白字）因节假日格内班次字色编码丢失而落选。
      两版各 8 张模拟器截图对比（`cli auto` + `miniprogram-automator`），
      见 `doc/changes/2026-09-21-160500-calendar-colors.md`
- [x] ~~**评估要不要换现成的日历组件**~~ —— **2026-09-21 评估完毕：保留自研，不换**。
      理由：① 现成组件都要改造成「每格显示自定义班次文字 + 节假日标记」，改造成本不低于自研；
      ② 自研的班次算法通过 `calendar.onDate()` 原样复用，不存在第二套逻辑；
      ③ 本次配色改造只动了组件的 `.wxml` 结构钩子与 `.wxss`，8 个倒班页零改动 ——
      自研组件的扩展性实测够用。真要换的前提（自研维护不动）目前不存在
- [x] ~~**五班三倒总览的「一值/二值/三值」与个人页「后夜(零点)/白班/前夜」的对应关系需要作者确认**~~ ——
      **2026-09-22 作者确认：映射正确（一值↔后夜、二值↔白班、三值↔前夜），保持现状不改**。
      逐 remainder 验算过两页在数学上完全一致；业务语义的"别扭感"（通常顺序是白班→前夜→后夜）
      属于行业习惯差异，现场就叫这个
- [ ] 以后若真需要用户头像昵称，用官方的「头像昵称填写能力」
      （`open-type="chooseAvatar"` + `input type="nickname"`），不要再回到 `wx.getUserInfo` / `getUserProfile`
- [ ] 若哪天要重做推送，用微信官方的**订阅消息**（`wx.requestSubscribeMessage` + 服务端下发），
      比自建邮件/短信链路省事，也不用收集邮箱手机号
- [ ] 若哪天要重做自定义规则，规则的创建与发码要放在服务端做，
      不要依赖客户端写入（客户端凭据天生公开，任何人都能改 `RuleKey.state`）

### 2.4.0 发版收尾时记下的（2026-09-14）

- [x] ~~**README 的 2.4.0 日志里有两条「修了又被删」**~~ —— **2026-09-22 核实：本条已过时，无需再修**。
      README 内联日志已于 #51（2026-09-14）整体收敛到 `doc/releases/`；在那份 `2.4.0.md` 里
      这两条本就不在「修复」表内，只在文末注解里如实说明「修复后该页在本版随链路删除」
      ——当时担心的"自相矛盾"已经不存在。注解是有价值的历史记录，保留
- [ ] **8 个倒班页高度雷同，是仓库里最大的一块重复**：`worker.js` 308~352 行、
      `director.js` 251~309 行，结构基本一致。行数门禁（500 行）现在拦不住它们。
      月日历已经示范过正确的抽法（`utils/calendar.js` + `components/shift-calendar/`，
      页面只留一个 `getDayCell()`）；班次查询本身也能照这个思路收成
      「一个公共模块 + 每页一张配置表（锚点 / 周期 / 班组循环表 / 文案）」
- [ ] **行数门禁上限 500 是留了余量的，不是照现状卡的**：当前最大业务文件
      `pages/wbsd/worker/worker.js` 352 行。想更早逼出重构可调到 400（仍全绿），
      调到 350 以下会立刻红。改的地方是 `test/hygiene.test.js` 的 `MAX_FILE_LINES`
