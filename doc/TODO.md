# 待办

## 1. 经警队排班（上一天休三天）—— 已实现（已校准）

**状态**：代码已实现并校准（`pages/jjd/worker|director`，首页「中 油 电 能」下新增入口）：

- [x] 班组数：**4 个班**（一班~四班）—— 已确认（2026-09-11）
- [x] 排班基准：**2026-09-10 = 一班上班** —— 已确认（2026-09-11）
- [x] 班组切换：个人页顶部按钮（一班~四班）随时切换，打开默认一班（沿用现有页面做法）；「设置默认班组」暂不做
- [x] 首页单位名「热 电 厂」→「中 油 电 能」已单独一次变更完成（见 `doc/changes/2026-09-11-index-unit-rename.md`）

**规则**：上一天休三天 —— 4 个班循环，每天恰好 1 个班上班。

**验收标准**：个人页切换班组后 7 天列表按「上 1 天休 3 天」循环；总览页每天恰好 1 个班上班（已用 Node 断言验证，见 `doc/changes/2026-09-11-jjd-shift.md`）。

**已知遗留**：`getTotalDays()` 沿用了各页原有的 `Math.abs()` 写法，锚点日**之前**的日期会算错班（本页锚点是 2026-09-10，很近，用户往前翻就会碰到）。待单独一次变更把 9 个倒班页一起修掉。

## 2. 倒班页面增加「日历」视图（与「一周列表」切换）—— 待细化

**需求（用户口述）**：现在倒班页（个人 / 总览）展示的是一周 7 行列表；希望增加一个月的日历格式，两种格式可切换，日历上能标出法定假期。

**待澄清**：
- [ ] 适用范围：只做新增页面（如经警队），还是全部倒班页（sbsd / sbbd / wbsd / diy）？—— 待定
- [ ] 法定假期数据来源：内置节假日 JSON（每年手工更新）/ 第三方接口？—— 待定（倾向内置 JSON）
- [ ] 日历格子里显示什么：上班 / 休息文字、颜色标记、假期名称？
- [ ] 切换交互：页面内按钮 or 顶部 tab？

**备注**：细节确认后再实现；属于对现有页面的行为改动，先不动代码。
