# 待办

## 1. 经警队排班（上一天休三天）—— 待信息确认后实现

**目标**：首页「热 电 厂」更名为「中 油 电 能」，并在其下新增「经警队（上一天休三天）」入口，含 个人 / 总览 两个页面。

**倒班规则**：上一天休三天。

**待确认（先留空，等信息）**：
- [ ] 经警队分几个班轮换？（4 个班 / 3 个班 / 不分班）—— 正在确认中，待补
- [ ] 排班基准：以「哪一天 + 哪个班上班」为推算起点 —— 待补
  - 占位方案（仅当急需上线时用）：`2026-09-11 = 一班上班`

**信息确认后的预计改动**：
- `pages/index/index.wxml`：标题「热 电 厂」→「中 油 电 能」；在「五班三倒」行下新增经警队一行按钮（个人 / 总览）
- `pages/index/index.js`：新增 `toJjdWorker` / `toJjdDirector` 跳转
- `app.json`：注册新页面
- 新增 `pages/jjd/worker/*`、`pages/jjd/director/*`（参照 `pages/sbsd/*` 页面结构）
- 排班锚点为各页面 `getTotalDays()` 里的基准日期（一行常量，改一行即可）
- 完成后按规范在 `doc/changes/` 补一条变更记录

**验收标准**：个人页切换班组后，7 天列表按「上 1 天休 3 天」循环；总览页每天恰好显示 1 个班上班。

## 2. 倒班页面增加「日历」视图（与「一周列表」切换）—— 待细化

**需求（用户口述）**：现在倒班页（个人 / 总览）展示的是一周 7 行列表；希望增加一个月的日历格式，两种格式可切换，日历上能标出法定假期。

**待澄清**：
- [ ] 适用范围：只做新增页面（如经警队），还是全部倒班页（sbsd / sbbd / wbsd / diy）？—— 待定
- [ ] 法定假期数据来源：内置节假日 JSON（每年手工更新）/ 第三方接口？—— 待定（倾向内置 JSON）
- [ ] 日历格子里显示什么：上班 / 休息文字、颜色标记、假期名称？
- [ ] 切换交互：页面内按钮 or 顶部 tab？

**备注**：细节确认后再实现；属于对现有页面的行为改动，先不动代码。
