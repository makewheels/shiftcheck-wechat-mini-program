# 2026-09-12 01:18 发布准备 2.5.0：版本号、更新日志、按删除后的现状重写文档

## 背景/目的

前两个变更删掉了整条推送链路（5 个页面）与整条 DIY 链路（4 个页面），注册页面从 20 降到 11，
小程序收敛成「纯离线查班工具」。这是一次功能面的重大收缩，需要：

1. 升版本号并写清更新日志（否则用户和以后的自己都不知道这些功能为什么不见了）
2. 把 `doc/` 里所有按 20 个页面写的描述改成 11 个页面的现状
3. 把这一轮明确决定「先记着、之后再修」的问题**如实记进 `doc/TODO.md`**，不能只留在对话里

## 改动清单

- `app.js`：`globalData.appVersion` `2.4.0` → **`2.5.0`**（设置页底部会显示）
- `README.md`：新增 `## 2.5.0` 更新日志，写明删了哪两条链路、**为什么**（各自的服务端/数据侧证据）、
  页面数变化、隐私接口归零、广告位 3 → 1，以及顺带消掉的 5 类问题
- `doc/TODO.md` 大改：
  - 第 1、2、3 条（经警队排班、月日历、强制升级）标注为已完成，第 2 条的接入范围从
    「9 个页面（含 diy）」更正为 **8 个**
  - 第 4 条「发布前必须在微信后台做的事」按现状重写：**隐私保护指引已无必须声明的接口项**、
    广告位只剩 1 个、域名只剩 1 个，并补上 LeanCloud ACL、ip138 token 作废、GitHub Support 工单
  - **新增第 5 条「已删除的功能（记录备查）」**：一张表列清 2.4.0 与 2.5.0 各删了什么、为什么删，
    免得以后有人以为是漏掉了；同时说明 LeanCloud 后台的表**没删**，数据还在
  - **新增第 6 条「已知遗留」**：本轮明确决定先不修的 11 项，按性价比排序，每项都写清现象、
    证据与最小修法（转发功能全死、11 个页面缺 `navigationBarTitleText`、默认班组只对五班三倒生效、
    四班四倒/三班半倒缺日期跳转、首页 2 处 `<span>`、首页统计口径失真、
    **断网时首页弹「登录没成功」**、五班三倒一值/二值/三值的语义待作者确认等）
- `doc/发布前检查单.md`：
  - 第 1 节补充「只有首页统计在用它，8 个倒班页与设置页纯本地、断网也能查班」
  - 第 2 节重写：**已无任何微信隐私接口**，只需如实说明设备信息与 openid 的收集；
    并列出历次「不再收集」的项（剪贴板静默上报与 IP → 2.4.0；剪贴板读取 → 2.5.0；
    邮箱手机号 → 2.5.0）
  - 第 4 节：广告位 3 → 1，另两个随页面删除；补充「`<ad>` 都没有 `binderror`，拉不到广告时底部留白」
  - **第 9 节真机自测清单按 11 个页面重写**，分成「首页与入口 / 倒班页（核心，重点验）/
    离线与登录 / 其它」四组；删掉所有已不存在页面的自测项；
    明确标注「跳转到指定日期只有五班三倒与经警队有」，
    并把「断网时首页会弹登录没成功」写成**已知待修项**而不是预期行为
- `AGENTS.md`：隐私章节改成「2.5.0 之后零隐私接口，`hygiene.test.js` 零容忍守着」；
  补一条「8 个倒班页与设置页是纯本地计算，不要让它们开始依赖网络或登录态」；
  目录说明里的页面清单从「含 diy、setting/*」改成实际的 11 个页面
- `doc/README.md`：本仓库速览里的页面描述与倒班页数量同步更正

## 验证方式

- `node --test test/*.test.js`：**130 项全部通过**
- `hygiene.test.js` 的「`app.js` 版本号必须等于 README 更新日志里最新版本号」断言通过
  （两边都是 2.5.0；这条门禁就是为了防止只改一边）
- 文档里的每个数字都来自实测检索，不是估的：注册页面 11、`duration: 20000` **0 处**
  （15 处全在已删页面里，所以"卡 20 秒"问题随删除自动消失）、`showShareMenu` 11 处、
  `onShareAppMessage` 0 处、`navigationBarTitleText` 1 处（只有 `app.json` 全局那条）、
  广告位 1 个、`<span>` 2 处、隐私接口 0 处、LeanCloud 使用点 2 个文件、
  date picker 只在 wbsd 与 jjd 的 4 个页面
- 文档路径引用由 `structure.test.js` / `hygiene.test.js` 校验（变更记录文件名格式、路径存在性）
- 界面级验证待补（开发者工具 CLI 服务端口未开启）

## 待确认项

- 版本号用 **2.5.0**：删掉两条完整功能链路属于用户可见的重大变化，按语义化版本升 minor。
  2.4.0 的 tag 与 Release 已经在 GitHub 上，所以不往回改，直接往前升
- LeanCloud 后台的 5 张表（`WechatUser` / `PushMission` / `Rule` / `RuleKey` / `UserRule`）
  本次都没动。是否清理、什么时候清理由作者决定
- 第 6 节里排在最前的一条（断网弹「登录没成功」）建议**在上传微信之前**就修掉：
  如果发布时漏配 request 域名白名单，所有用户一打开小程序就会看到这个弹窗

## 回滚方式

- revert 本提交即回到 2.4.0 的版本号与文档；代码逻辑不受影响（本 PR 只改版本号与文档）
