# 2026-09-22 20:50 发布准备：版本号 2.6.0、发布说明、检查单版本

## 背景/目的

v2.5.0（2026-09-15）之后 master 又合入 7 个 PR（#54~#60，2026-09-21 的一整批），
均不在任何 tag/release 里。用户决定发版，按仓库规范走「发布说明先进仓库，
再同步 GitHub Release」：bump 版本号 → PR → tag → Release → 命令行上传微信。

版本号取 **2.6.0**（minor）：这批含功能增强（转发接入、月日历配色、默认班组
四种倒班全生效、日期跳转补齐），不是 patch 体量；v2.5.0 的 Release 已发布一周，
不移动已发布的 tag。

## 改动清单

- `app.js`：`globalData.appVersion` `2.5.0` → **`2.6.0`**（设置页底部显示）
- `doc/releases/2.6.0.md`：新增本版发布说明（汇总 #54~#60 的用户可见变化）
- `doc/发布前检查单.md`：标题版本 2.5.0 → 2.6.0（正文的历史事实引用不动）
- 不改任何逻辑代码

## 验证方式

- `node --test test/*.test.js` 全绿（含 hygiene 的「appVersion 与 doc/releases
  最新文件一致」双向咬合门禁）
- PR CI（Node 24 单版本）绿后合并

## 待确认项

- 上传微信用 `cli upload --version 2.6.0`，上传成功后另行在 TODO 第 4 节记录

## 回滚方式

`git revert` 即可；tag/release 尚未创建，无需处理
