# 2026-09-21 15:34 「个人模式默认班组」四种倒班都接上

## 背景 / 目的

`doc/TODO.md` 第 6 节记的遗留：设置里存的默认班组（`setting-wbsdDefault`）
**只有五班三倒的个人页在读**，经警队 / 四班四倒 / 三班半倒的个人页都是硬编码
`setBanzu1()`，但设置入口的文案「个人模式默认班组」是通用的 —— 名不副实。

两个修法（改文案 / 全接上）问过用户，**选定：四种倒班都接上**（2026-09-21）。

## 改动清单

- `pages/setting/workerDefaultBanzu/workerDefaultBanzu.js` 重写成数据驱动：
  `MODES` 配置四种倒班（storage key、班组列表、未设置时的兜底），
  `readModes()` 读 storage 拼渲染行，`changeBanzu(e)` 按 `data-index` 定位到某一种倒班，
  动作表选中后写 storage、整表刷新、toast 提示带倒班名
- 对应 `.wxml`：`wx:for` 渲染四行「倒班名：当前值 + 修改默认班组按钮」；
  `.wxss` 加 `.mode-block` 分组间距，广告上边距从 500rpx 收到 60rpx
  （原来整页只有一组设置靠大留白把广告推到屏下，现在四组内容本身够长）；
  顺带删掉从未被 wxml 用过的死样式 `.flex-wrap`
- `pages/jjd|sbsd|sbbd/worker/worker.js` 的 `onLoad`：`setBanzu1()` 硬编码改成
  读各自的 `setting-jjdDefault` / `setting-sbsdDefault` / `setting-sbbdDefault`
  （`parseInt` 后 1..N 合法则 `setBanzuN`，否则兜底一班 —— 与接之前的行为一致）
- **五班三倒个人页未动**：它的 onLoad 链（未设置/0 → 三班，其余 → 对应班）是历史行为，保持原样
- 测试沙箱 `test/helpers/miniprogram.js`：`makeWxStub` 增加 `showActionSheet`
  支持（`opts.actionSheetTap` 控制选中项），供设置页测试用
- 新增 `test/default-banzu.test.js`（15 条）：4 个个人页「设了就用 / 没设走兜底 /
  越界垃圾值走兜底」（五班三倒只测前两条，其历史链条不在本 PR 范围）；
  设置页四行显示、修改动作写对 key 且不影响其它倒班
- `AGENTS.md` / `doc/README.md`：测试文件清单九个→十个
- `doc/TODO.md` 勾掉对应条目

## 验证方式

- `node --test test/*.test.js` 全绿（新增 15 条，共 169）
- 排班输出不受影响：金标准快照、锚点守卫、周期性不变量等既有测试全部原样通过
- **界面层未验**：设置页四行布局、动作表弹出与 toast 需模拟器/真机过一遍

## 待确认项

- 兜底值说明文案「未设置（默认三班）」是本次新加的显示，原来只显示「未设置」；
  如果不想要括号说明可以再去掉

## 回滚方式

- `git revert` 本提交即可；storage 里新增的三个 key 留着无害（无页面再读就是死数据，
  用户重新设置会覆盖）
