# 2026-09-11 新增经警队「上一天休三天」排班（个人/总览）；首页「热电厂」更名「中油电能」

## 背景/目的

- 首页「热 电 厂」更名为「中 油 电 能」。
- 其下新增「经警队（上一天休三天）」入口：个人 / 总览两个页面。

## 改动清单

- 新增 `pages/jjd/worker/*`、`pages/jjd/director/*`：7 天列表、班组切换（一班~四班）、日期翻页 / 跳转指定日期
- `pages/index/index.wxml`：「热 电 厂」→「中 油 电 能」；在「五班三倒」行下新增经警队一行按钮（个人 / 总览）
- `pages/index/index.js`：新增 `toJjdWorker` / `toJjdDirector` 跳转
- `app.json`：注册两个新页面
- `doc/TODO.md`：第 1 条状态更新为「已实现（已校准）」

## 排班规则与基准（已确认）

- 规则：上一天休三天 —— 4 个班循环，每天恰好 1 个班上班。
- 班组数 = 4（一班~四班）—— 2026-09-11 已确认。
- 基准日 = **2026-09-10「一班上班」**（"从昨天起为第一天"）—— 2026-09-11 已确认。
- 对应代码：`pages/jjd/worker/worker.js`、`pages/jjd/director/director.js` 的 `getTotalDays()` 基准日。

## 验证方式

- 本机未安装微信开发者工具，无法真机预览；采用 Node 机器验证：
  - 用假 `Page()` 加载两个页面 js，对 2026-09-10 起 40 天 × 4 个班组断言：每天恰好 1 个班上班；每班上班间隔恒为 4 天（上 1 休 3）；总览页显示的班与个人页一致；基准日（2026-09-10）为一班上班。
  - `node --check` 语法校验、`jq empty` JSON 校验、`app.json` 页面注册核对。
  - 结果：全部通过（校准基准日后重跑通过）。

## 回滚方式

- revert 本次提交；无数据 / 后端改动。
