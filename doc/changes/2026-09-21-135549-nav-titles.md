# 2026-09-21 13:55 给 11 个页面补 navigationBarTitleText

## 背景 / 目的

`doc/TODO.md` 第 6 节记的遗留：11 个页面的 json 都没有 `navigationBarTitleText`，
标题栏全部退回 `app.json` window 里的全局标题「查班神器」。用户从首页进入五班三倒个人页、
再切到总览页，标题栏一个字都不变，多级页面分不出自己在哪页。

## 改动清单

- 11 个页面 json 各加一行 `navigationBarTitleText`：
  - 首页：查班神器（与全局标题一致，显式写出）
  - 8 个倒班页：`五班三倒 / 经警队 / 四班四倒 / 三班半倒 · 个人 / 总览`
  - 设置主页：设置
  - 个人模式默认班组页：默认班组
- `test/structure.test.js` 新增门禁：app.json 注册的每个页面 json 必须有非空
  `navigationBarTitleText`，新页面漏配会直接红
- `doc/TODO.md` 勾掉对应条目

## 验证方式

- `node --test test/*.test.js` 全绿（新增 1 条，共 136）
- 标题文案与页面内容一一人工核对（个人/总览、四种倒班方式名称与首页入口文案一致）

## 待确认项

- 标题栏底色与文字色沿用全局配置（浅蓝底黑字），本 PR 不动

## 回滚方式

- `git revert` 本提交即可，纯配置改动，无状态、无数据影响
