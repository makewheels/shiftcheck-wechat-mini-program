# 2026-09-11 经警队文案去掉括号说明（首页 + 个人页 + 总览页）

## 背景/目的

- 经警队入口/页面标题原为「经警队（上一天休三天）」，与同页其它倒班方式（五班三倒 / 四班四倒 / 三班半倒）的命名风格不一致，且「上一天休三天」属于规则说明，不需要在入口和页面标题里重复。
- 按要求：首页、经警队「个人」页、「总览」页全部去掉括号，只保留「经警队」。

## 改动清单

- `pages/index/index.wxml`：`<text>经警队（上一天休三天）：</text>` → `<text>经警队：</text>`
- `pages/jjd/worker/worker.wxml`：副标题 `经警队（上一天休三天）：{{modeName}}模式` → `经警队：{{modeName}}模式`
- `pages/jjd/director/director.wxml`：副标题同上，去掉括号（第二次提交补上，个人/总览两页文案现已一致）
- 无 js / json / 样式改动，未触碰任何排班逻辑与基准日。
- 排班规则「上一天休三天」本身不变，仍由 `pages/jjd/*/*.js` 的 `getTotalDays()` 实现；本次只是不再把规则写进标题文案。

## 验证方式

- `git diff` 确认三处各 1 行文本变化；`git grep 经警队 -- pages/` 确认已无残留括号写法。
- 微信开发者工具 2.02.2608070 监听文件变更后自动重编译，`WeappLog` 中无编译错误、无 `not found in vendor contents map`（本机基础库离线加载问题已同日修复，模拟器可正常渲染）。
- 未做真机预览；文案的最终目视确认由使用者在模拟器看一眼即可。

## 待确认项

- 无（总览页是否去括号已确认：去）。

## 回滚方式

- revert 对应提交；纯文案改动，不涉及数据、排班基准日与后端。
