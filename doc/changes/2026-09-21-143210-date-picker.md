# 2026-09-21 14:32 四班四倒 / 三班半倒的 4 个页面补「跳转到指定日期」

## 背景 / 目的

`doc/TODO.md` 第 6 节记的遗留：8 个倒班页里只有五班三倒和经警队的 4 个页面有
`<picker mode="date">` 日期跳转，四班四倒（sbsd）与三班半倒（sbbd）的个人/总览 4 个页面没有，
同样的功能在不同倒班方式下时有时无。

## 改动清单

- `pages/sbsd/worker|director`、`pages/sbbd/worker|director` 的 wxml 各加一段
  `<picker mode="date" bindchange="bindDateChange">`（插在月日历组件之后、
  「前 一 天」按钮组之前，与五班三倒页的位置一致）
- 4 个页面 js 各加 `bindDateChange`，**逐字抄自 `pages/wbsd/worker/worker.js`**
  （拆 value、setData 年月日、`setText()` 重算列表、toast 提示）；
  个人页与总览页的 `getWork()` / `getDayText()` 不动，班次算法不存在第二套
- `test/structure.test.js` 新增门禁：8 个倒班页的 wxml 都必须有该 picker
- `test/shift-pages.test.js` 新增「日期跳转」一组（8 页各 1 条）：
  picker 的 value 是 1 基月份、页面 data 是 0 基（要减 1）、跳转后列表第一行
  与「直接 setData 到同一天」完全一致
- `doc/TODO.md` 勾掉对应条目

## 验证方式

- `node --test test/*.test.js` 全绿（新增 9 条，共 152）
- 新增的 8 条功能测试覆盖了 1 基→0 基月份转换这个最容易错的点
- **界面层未验**：picker 弹出的日期选择器样式与 toast 手感需模拟器/真机过一遍

## 待确认项

- 无

## 回滚方式

- `git revert` 本提交即可，4 个页面回到没有日期跳转的旧状
