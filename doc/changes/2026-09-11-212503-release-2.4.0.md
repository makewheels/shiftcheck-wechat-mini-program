# 2026-09-11 21:25 发布准备：版本号 2.4.0、更新日志、待办整理与发布前检查单

## 背景/目的

前面几个 PR 把功能与缺陷都处理完了，这一条只做「发布准备」：把版本号、更新日志、
待办状态和发布前要在微信后台做的事落定，方便按检查单提审。

## 改动清单

- `app.js`：`globalData.appVersion` `2.3.4` → **`2.4.0`**（设置页底部会显示这个版本号）
- `README.md` 版本更新日志：补上漏记的 **2.3.4**（leancloud 专有域名），新增 **2.4.0** 条目
- `doc/TODO.md`：
  - 第 1、2、3 条状态更新为已完成（经警队排班、月日历视图、强制升级），各自保留遗留待办
  - 新增第 4 条「发布前必须在微信后台做的事」
  - 新增第 5 条「其它已知遗留（不阻塞发布）」：feedback 空页面、logs 模板页、
    `updateMail`/`updatePhone` 的 `users[0]` 未判空、没有页面实现 `onShareAppMessage`、
    首页硬编码的作者 openid、日后若需要头像昵称应改用官方「头像昵称填写能力」
- `doc/README.md` 本仓库速览：补充 `utils/shift.js`（有符号天数差与取模的约定）、
  月日历三件套（`utils/calendar.js` / `components/shift-calendar/` / `utils/holiday.js`，
  含「节假日数据每年要手工补」的提醒）、取 openid 的统一约定、发布检查单入口
- 新增 `doc/发布前检查单.md`：域名白名单、隐私保护指引（逐项列出要声明的信息类型与代码位置）、
  ip138 token 作废、广告位归属、基础库版本、服务类目、上传提审步骤、真机自测清单

## 本次发布的事实核对（检查单里的数字都来自全仓库检索）

- **网络域名只剩 1 个**：`https://api.leancloud.mp.shiftcheck.work`（`app.js` 的 `AV.init`）。
  `wx.request` / `downloadFile` / `uploadFile` / `connectSocket` 在业务代码里已无其它调用
- **广告位 3 个**：`adunit-499feca899514736`（banner，17 处；首页那处是注释掉的）、
  `adunit-d5a2055026c60752`（banner，diy 页）、`adunit-79ec0f8be5efc0f1`（激励视频，diyPush）
- **隐私接口只剩 1 个**：`wx.getClipboardData`（`importRuleByKey.js`，用户点「一键粘贴」时触发）
- **会写入后端的个人资料**：邮箱、手机号（用户自己在「我的账户」里填）、openid、设备与网络信息

## 验证方式

- `node --check` app.js 通过；`app.json` 与全部页面 json 解析通过
- 结构完整性检查通过：22 个注册页面的 js/json/wxml 齐全且非空、
  代码里所有 `navigateTo`/`redirectTo` 目标都在 `app.json` 注册、
  `usingComponents` 声明的组件文件齐全且带 `"component": true`、
  wxml 里 `bind*` 绑定的处理函数在对应 js 中都存在
- 排班回归（与上一个 PR 逐日比对，0 差异）、日历 8179 项、周期性 2391 项断言全部通过
- 文档交叉引用检查：`doc/TODO.md`、`doc/README.md`、`doc/发布前检查单.md` 与
  `doc/changes/` 里提到的路径均真实存在

## 待确认项

- 版本号用 `2.4.0`（新增月日历与经警队排班属于新功能，按语义化版本升 minor）。
  如果想跟微信后台已有的版本序列对齐，改成别的号只需要动 `app.js` 一行 + README
- 检查单第 8 节的真机自测项需要人工过一遍，本机开发者工具 CLI 服务端口未开启，
  无法用命令行编译/预览

## 回滚方式

- revert 本次提交；纯文档与版本号改动，不影响任何逻辑
