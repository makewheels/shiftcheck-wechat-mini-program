# 2026-09-12 00:07 补全 README 的 2.4.0 更新日志（含 tag 之后合入的修复）

## 背景/目的

`v2.4.0` 的 tag 打在了「删除两个死页面」那个提交上，之后又合入了 6 个变更
（diyPush 页面配置、位运算笔误、sitemap 收紧、homepage 死键、首页假节流、返回主页深链）。
tag 要移到当前 master，那么 **README 的 2.4.0 更新日志必须覆盖这些内容**，
否则用户看到的版本说明与实际发布的代码不一致。

## 改动清单

- `README.md` 的 `## 2.4.0` 段落补上 7 条：
  - 修复「自定义规则提醒」页打不开（页面 json 被误标成自定义组件，v2.3.2 起就有）
  - 修复「返回主页」在深链进入（搜一搜 / 扫码 / 分享卡片）时点了没反应（9 个页面）
  - 修复首页那个从来没生效过的「5 分钟内不重复上报」节流（时间戳原来存在页面 data 里）
  - 修复 pushHome 把位运算 `&` 当逻辑与 `&&` 用
  - 收紧 sitemap：只开放首页与 8 个倒班查询页，含手机号 / 邮箱输入的页面不再可被搜到
  - 清理死代码一条补全：未使用的 LeanCloud 打包版 SDK、feedback 空占位页、logs 模板页、
    只写不读的 homepage 设置
  - 工程化两条：`test/` 测试套件（136 项，零依赖）+ GitHub Actions 门禁（已设为 master 必需检查）、
    `AGENTS.md` 与变更记录文件名带时分秒
- 只改 README，不动 `app.js` 的 `appVersion`（仍是 `2.4.0`，与本段日志一致，
  `test/hygiene.test.js` 有一条断言专门守这个一致性）

## 验证方式

- `node --test test/*.test.js`：**136 项全部通过**，其中「`app.js` 的版本号与 README
  更新日志里最新的版本一致」这条断言正是在守这类不同步
- 逐条核对：新补的 7 条都能在 `doc/changes/` 里找到对应的变更记录
  （`232926-fix-diypush-page-json`、`233224-fix-bitwise-and-typo`、`234256-sitemap-restrict`、
  `234810-remove-homepage-key`、`235559-fix-report-throttle`、`000522-fix-back-home-deeplink`）
- README 渲染检查：条目仍在 `## 2.4.0` 段落内，未越界到 `## 变更规范`

## 待确认项

- 更新日志的粒度：现在 2.4.0 一段有 16 条，偏长。如果希望 README 只留用户可见的变化，
  可以把工程类条目（测试套件、CI、AGENTS.md）挪到 `doc/` 下，README 只留功能与修复
- 以后发版的顺序建议：**先把更新日志写全 → 再打 tag → 再建 GitHub Release**，
  这样 tag 与日志天然对齐，不用像这次一样回头补

## 回滚方式

- revert 本提交；README 回到只覆盖 tag 之前内容的状态
