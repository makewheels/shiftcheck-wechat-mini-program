# 2026-09-22 20:53 补全 2.5.0 发布说明：并入 v2.5.0 tag 之后合入的 7 个 PR

## 背景/目的

v2.5.0 tag 与 GitHub Release 是 2026-09-15 「准备 2.5.0 开发版本」（#53）时打的，
但 2.5.0 尚未上传微信、未提审发布，开发仍在继续：2026-09-21 又合入 7 个 PR
（#54~#60），都还没进发布说明。本版要发的是这些改动的总和，发布说明需要如实覆盖。

（此前一次误开的「2.6.0」PR #61 已关闭废弃 —— 版本没发出去就不另开新版本号。）

## 改动清单

- `doc/releases/2.5.0.md`：并入 #54~#60 的用户可见变化（转发接入、月日历配色、
  默认班组四种倒班全生效、日期跳转补齐、导航栏标题、span 修复），
  标题与验证章节同步更新；LeanCloud 移除的既有描述保留
- 不改任何代码；`appVersion` 维持 2.5.0

## 后续动作（不在本 PR 内，记录备忘）

- 合并后把 `v2.5.0` tag 移到合并提交（原 tag 停在 #53，不含这批改动）
- `gh release edit v2.5.0 --notes-file doc/releases/2.5.0.md` 重新生成 Release 正文
- `cli upload --version 2.5.0` 上传开发版本

## 验证方式

- `node --test test/*.test.js` 全绿（hygiene 门禁校验发布说明存在且不指向 master）
- 发布说明的链接规范由 `hygiene.test.js` 钉着

## 回滚方式

`git revert` 即可
