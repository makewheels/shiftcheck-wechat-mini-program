# 2026-09-22 21:25 基础库版本统一到 2.25.3

## 背景/目的

TODO 第 4 节：`project.config.json` libVersion 2.8.2 与 `project.private.config.json` 2.25.3
不一致（发布前检查单列的待办）。作者在三个选项（2.25.3 / 升 3.17.3 / 只设后台最低版本）里
选定 **2.25.3**：与真机当前实际使用一致，保守稳妥；本机虽有 3.17.3，但升级后全部表现
要重新验一轮，不值得在发版关口引入这个变量。

## 改动清单

- `project.config.json`：`libVersion` `2.8.2` → `2.25.3`
- `doc/TODO.md` 第 4 节：勾掉基础库条目

## 验证方式

- `node --test test/*.test.js` 全绿（`project.config.json` 的 libVersion 不参与测试断言，
  structure 门禁只校验 appid 与 json 可解析）
- 改动只影响开发者工具打开项目时用的基础库；**代码包内容不变，已上传的开发版本无需重传**

## 回滚方式

`git revert` 即可
