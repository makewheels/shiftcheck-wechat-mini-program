# 查班神器 2.2.3：新增推送任务页与 MTA 统计

> 本文件是 GitHub Release [v2.2.3](https://github.com/makewheels/shiftcheck-wechat-mini-program/releases/tag/v2.2.3) 的正文来源。
> 改这里，然后用 `gh release edit v2.2.3 --notes-file doc/releases/2.2.3.md` 同步过去。

## 新增

- `pages/setting/newPushMission/` —— 新增推送任务页（4 个文件，334 行 js）
- `libs/mta_analysis.js` —— 腾讯 MTA 统计

> 后续：截至 2.4.0，`pushHome` 的「新增推送任务」按钮里仍有一句裸 `return` + toast「开发中」，
> 所以这个页面实际进不去（是否接通待定，因为它依赖的 `Rule` 表没有任何写入方）；
> MTA 统计在 2.3.3 从代码里移除，死文件本身留到 **2.4.0** 才删。

---
> **补录说明**：本 Release 于 2026-09-12 补建 —— 此前仓库只有 tag、从未建过任何 Release。
> 提交日期均为 **2021-05-28**（这段历史是一次性导入的），各版本**真实发布日期已无法还原**；
> GitHub 显示的发布时间是补录时间。说明内容是事后从提交 diff 整理的，非当时的发布说明。
