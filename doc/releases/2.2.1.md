# 2.2.1

> 本文件是 GitHub Release [v2.2.1](https://github.com/makewheels/shiftcheck-wechat-mini-program/releases/tag/v2.2.1) 的正文来源。
> 改这里，然后用 `gh release edit v2.2.1 --notes-file doc/releases/2.2.1.md` 同步过去。

## 推送页改造

- `pushHome` 增加「邮件剩余：N 封」「短信剩余：N 条」的显示
- 「设 置 推 送」按钮改为「新增推送任务」，并加了「已有推送任务：」占位（列表本身一直没做出来）
- **修掉 `bindchange="smsSwitch "` 的尾部空格笔误** —— 事件名带空格时绑定是失效的，短信开关此前点了没反应
- 首页与设置页的版本号文案更新到 2.2.1
- `diy` / `accountHome` / `home` / `importRuleByKey` / `myRuleHome` / `updateMail` / `updatePhone` 均有调整

---
> **补录说明**：本 Release 于 2026-09-12 补建 —— 此前仓库只有 tag、从未建过任何 Release。
> 提交日期均为 **2021-05-28**（这段历史是一次性导入的），各版本**真实发布日期已无法还原**；
> GitHub 显示的发布时间是补录时间。说明内容是事后从提交 diff 整理的，非当时的发布说明。
