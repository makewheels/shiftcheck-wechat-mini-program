# 2.3.3：删除 mta、修复 leancloud

> 本文件是 GitHub Release [v2.3.3](https://github.com/makewheels/shiftcheck-wechat-mini-program/releases/tag/v2.3.3) 的正文来源。
> 改这里，然后用 `gh release edit v2.3.3 --notes-file doc/releases/2.3.3.md` 同步过去。

## 原 README「版本更新日志」原文（该节 2026-09-14 已移除，原文保留在此）

删除 mta，修复 leancloud，修复登录 onShow 函数显示过快报 toJSON 错误问题

## 包含提交

- `2ae248d` 删除mta，修复leancloud
- `b555f77` test
- `8fe2478` Remdme-file ← **tag 指向这里**

## 后续更正（2026-09 复核）

- 那次「修复登录 onShow 报 toJSON 错误」实际只是把首页上报延后 800ms（`setTimeout`），
  **把竞态掩盖了而没有解决**：其余 20 多处 `AV.User.current().toJSON()` 仍在，
  首次冷启动登录未回来时照样 `null.toJSON()` 崩页。**2.4.0** 才统一收口到
  `app.getOpenid()` / `app.withOpenid()` 并加登录重试与页面入口守卫
- MTA 当时只从代码里移除，`libs/mta_analysis.js` 文件本身留到 **2.4.0** 才删

---
> **补录说明**：本 Release 于 2026-09-12 补建（此前仓库只有 tag、从未建过 Release），
> GitHub 显示的发布时间是补录时间。上面的条目是作者当年写在 `README.md`「版本更新日志」里的原文（该节 2026-09-14 已移除，原文保留在此），
> 「后续更正」是 2026-09 复核代码时补的。
