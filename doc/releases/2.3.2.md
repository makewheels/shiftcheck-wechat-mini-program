# 2.3.2：DIY 规则推送与激励视频加次数

> 本文件是 GitHub Release [v2.3.2](https://github.com/makewheels/shiftcheck-wechat-mini-program/releases/tag/v2.3.2) 的正文来源。
> 改这里，然后用 `gh release edit v2.3.2 --notes-file doc/releases/2.3.2.md` 同步过去。

## 原 README「版本更新日志」原文（该节 2026-09-14 已移除，原文保留在此）

- 在 diy 页面中设置 banner2 广告
- 在 diy 规则中添加推送功能，并且添加看激励视频增加推送次数功能

## 代码层面

- 新增 `pages/diyPush/`（4 个文件）：自定义规则提醒 + `wx.createRewardedVideoAd` 看视频加次数
- `pages/diy/diy.wxml` 加入 `adunit-d5a2055026c60752`
- 移除 `pages/index/index.js` 里 16 行

## 后续更正（2026-09 复核）

- 本版本引入的 `pages/diyPush/diyPush.json` 写了 `"component": true`，而 `diyPush.js` 用的是 `Page()` ——
  页面注册不上、跳过去白屏。这个错误一直留到 **2.4.0** 才被发现并修复，
  说明这条变现路径此前很可能从未在真机上验证过

---
> **补录说明**：本 Release 于 2026-09-12 补建（此前仓库只有 tag、从未建过 Release），
> GitHub 显示的发布时间是补录时间。上面的条目是作者当年写在 `README.md`「版本更新日志」里的原文（该节 2026-09-14 已移除，原文保留在此），
> 「后续更正」是 2026-09 复核代码时补的。
