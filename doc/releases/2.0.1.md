# 2.0.1

> 本文件是 GitHub Release [v2.0.1](https://github.com/makewheels/shiftcheck-wechat-mini-program/releases/tag/v2.0.1) 的正文来源。
> 改这里，然后用 `gh release edit v2.0.1 --notes-file doc/releases/2.0.1.md` 同步过去。

## 改动

- **移除**三班半倒的月视图页 `pages/sbbd/worker/worker-month.*`（4 个文件）
- 引入 LeanCloud SDK：`libs/av-weapp-min.js`
- 补上 `project.config.json`
- 相应调整 `app.js` / `app.json` / 首页

> 后续：这个 `av-weapp-min.js`（打包版 SDK）后来一直没被真正使用 —— 项目走的是
> `av-core-min.js` + `leancloud-adapters-weapp.js` 的组合，该文件在 **2.4.0** 被删除（165 KB）。

---
> **补录说明**：本 Release 于 2026-09-12 补建 —— 此前仓库只有 tag、从未建过任何 Release。
> 提交日期均为 **2021-05-28**（这段历史是一次性导入的），各版本**真实发布日期已无法还原**；
> GitHub 显示的发布时间是补录时间。说明内容是事后从提交 diff 整理的，非当时的发布说明。
