# 查班神器 2.2.0：账户、推送与 DIY 自定义规则

> 本文件是 GitHub Release [v2.2.0](https://github.com/makewheels/shiftcheck-wechat-mini-program/releases/tag/v2.2.0) 的正文来源。
> 改这里，然后用 `gh release edit v2.2.0 --notes-file doc/releases/2.2.0.md` 同步过去。

大版本：从「只能查班」扩展到「账户 + 推送 + 自定义规则」。

## 新增页面

- `pages/diy/` —— DIY 自定义规则
- `pages/setting/accountHome/` —— 我的账户
- `pages/setting/authUserInfo/` —— 微信快捷登录
- `pages/setting/importRuleByKey/` —— 激活码导入
- `pages/setting/myRuleHome/` —— 由 `myRule` 改名而来
- `pages/setting/pushHome/` —— 订阅上班推送
- `pages/setting/updateMail/`、`pages/setting/updatePhone/` —— 设置邮箱 / 手机
- `pages/setting/feedback/` —— 本次只提交了 `json` / `wxml` / `wxss`

## 其它

- 清空 `utils/util.js`

> 后续：`authUserInfo` 依赖的 `scope.userInfo` 授权微信在 2021 年就停止下发，该页在 **2.4.0** 被删除
> （它当时还把「我的DIY规则」和「订阅上班推送」两个入口挡死了）；`feedback` 一直是半成品，
> 也在 **2.4.0** 删除。

---
> **补录说明**：本 Release 于 2026-09-12 补建 —— 此前仓库只有 tag、从未建过任何 Release。
> 提交日期均为 **2021-05-28**（这段历史是一次性导入的），各版本**真实发布日期已无法还原**；
> GitHub 显示的发布时间是补录时间。说明内容是事后从提交 diff 整理的，非当时的发布说明。
